language: java

jdk:
  - oraclejdk8
  
build:
  ci:
   - mkdir -p shippable/testresults
   - mkdir -p shippable/codecoverage
  
   - cd mvn-parent
   - mvn install
   
   - cp -r target shippable/codecoverage
   - cp -r CiTestTest/target shippable/testresults